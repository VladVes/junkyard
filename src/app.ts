const message: string = 'Hello TS!';
console.log(message);

// const btn = document.querySelector('#btn'); // no error because we have added "DOM" to the lib [] in the tsconfig.json
// btn?.addEventListener('click', () => console.log('BTN HAS CLICKED!')) // btn? we need 